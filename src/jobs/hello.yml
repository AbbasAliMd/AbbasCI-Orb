description: >
  Sample description
# What will this job do?

executor: default

parameters:
  to:
    type: string
    default: "World"
    description: "Hello to whom?"
steps:
  - run:
      name: hello job step to check param as env
      environment:
        GET_TO: << parameters.to >>
      command: |
        echo "value of get_to $GET_TO"
        echo "Checking parameters passing as Bash Env"
